#include "headers.h"// Include required headers and definitions

u32 ColScan(void);// Scan and return active column
u32 RowCheck(void);// Check and return active row
u32 ColCheck(void);// Check and return column status
u8 KeyScan(void);// Scan keypad and return pressed key
void InitKPM(void);// Initialize keypad pins and settings
